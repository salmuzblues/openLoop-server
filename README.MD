# Getting started with Project

Node Version:
v16.14.2 (LTS)

First:
Run your mongoDb
In my case is:
### `mongod --dbpath=/Users/{user_mac}/data/db`

### `npm install`

Run the server 
### `npm start` 